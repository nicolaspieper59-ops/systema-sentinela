- name: Vérification des ressources minimales
        run: |
          if [ ! -f "flux_live.json" ]; then
            echo '{"utc": "FALLBACK", "jpl": {}, "astres": {}}' > flux_live.json
          fi
