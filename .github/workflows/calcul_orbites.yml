name: SENTINELA - RECONSTRUCTION FORCEE

on:
  schedule:
    # RECONSTRUCTION TOUTES LES HEURES À LA MINUTE 5 (Ex: 10:05, 11:05, 12:05...)
    - cron: '5 * * * *'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Code Checkout
        uses: actions/checkout@v4

      - name: Configuration de l'environnement Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Installation du module Requests
        run: pip install requests

      - name: Calcul des éphémérides (Execution Script)
        run: python calculer_orbites.py

      # ÉTAPE DE COMMIT SUPPRIMÉE POUR ÉVITER LES CONFLITS ET LES ERREURS DE DROITS

      - name: Configurer l'artefact Pages
        uses: actions/upload-pages-artifact@v3
        with:
          path: '.'

      - name: Déploiement Flash GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
